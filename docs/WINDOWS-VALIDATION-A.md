# Windows 验证批次 A：GameShelf 0.3

状态：待 Windows 实机执行。当前开发环境未连接 Windows，也未修改 `G:\GameShelf`、`G:\GalGame` 或 `G:\game`。

执行前复制整个便携版 `data` 文件夹，并记录 GameShelf 版本、Windows 版本和测试磁盘格式。使用专门测试目录，不直接对唯一一份游戏收藏做首次验证。

## 扫描与审核

- [ ] 分别选择单目录、多目录、空目录、无权限目录和包含 500 个以内候选的目录。
- [ ] 验证大小写不同但指向同一 `.exe` 的路径被判为重复，UNC 路径与跨盘路径不会崩溃。
- [ ] 验证卸载器、配置器、更新器、补丁备份和引擎辅助程序不会成为游戏候选。
- [ ] 验证同目录的原版/汉化版启动项合并到一个候选，跨目录版本只提示而不自动合并。
- [ ] 检查文件名、目录名和本地 `gameshelf.json` 三种名称来源均可审核、切换和手工修正。
- [ ] 断网扫描，确认流程无网络请求；导入已有游戏后再次扫描，确认不会覆盖用户编辑。

## 导入与整理

- [ ] 取消扫描、取消导入时数据库和游戏目录均无变化。
- [ ] 故意加入一个失效启动路径，确认整个批次拒绝且没有部分条目残留。
- [ ] 批量修改状态、分类、合集和隐私，重启后确认全部持久化。
- [ ] 验证发现的本地封面被复制到 `data\covers`，原图未移动；批量整理中的缺失封面检查可逐项替换。
- [ ] 在封面墙与信息列表间切换并重启，确认视图偏好保留。
- [ ] 用超长中日英混合标题检查封面墙、信息列表、搜索结果和详情页可读性。

## 回归与记录

- [ ] 安全视图开启时，批量导入中标记隐藏的条目不出现在主页和搜索结果。
- [ ] 启动每种导入方式产生的默认/备用启动项，确认工作目录正确。
- [ ] 运行 `npm run typecheck` 与 `npm test`，保存输出；记录所有失败候选及可复现目录结构。
