# Windows 验证批次 B：GameShelf 0.4

状态：Windows 实机回归最初为 17 Pass / 1 Fail；唯一缺陷 GS-WIN-001 已在 `d32f36c` 修复。修复后为 21 Pass / 0 Fail / 1 Skip，跳过的 junction 场景随后由独立 Windows 实机夹具通过。

以下清单保留为破坏性故障注入和大规模边界的扩展 Manual 验收，不需要为了 1.0 重复已经通过的核心回归。真实 `G:\GalGame` 与 `G:\game` 只做过只读扫描。

只使用可丢弃的测试游戏、Mod 和存档副本。执行前复制便携版 `data` 与测试存档，记录 GameShelf/Windows 版本、磁盘格式、杀毒软件和测试路径。

## 扩展 Manual：Package 安全管理

- [ ] 检测 BepInEx、MelonLoader、Mods、汉化补丁、内容补丁和修复目录；确认首次检测不修改游戏目录。
- [ ] 在预览和系统确认分别取消，确认目录名称、内容和数据库状态都不变。
- [ ] 停用后确认备份清单可校验、原目录变为 `.gameshelf-disabled`、文件数量/内容不变，游戏加载器不再加载该目录。
- [ ] 启用与“撤销上次变更”后确认目录恢复、变更日志完整，备份未被覆盖。
- [ ] 模拟目标路径已存在、源目录被占用、磁盘空间不足和杀毒软件锁定；确认操作拒绝或回滚，不删除文件。
- [ ] 验证 Package 位于游戏目录外、符号链接/联接或包含特殊文件时被拒绝。

## 扩展 Manual：存档快照与恢复

- [ ] 配置游戏目录内、`AppData`、`Documents` 和另一磁盘上的存档位置，创建多个分支与快照。
- [ ] 校验空目录、大量小文件、Unicode/超长文件名和接近 20 GiB 上限的明确错误。
- [ ] 修改快照中的一个字节，确认恢复在写入前被拒绝，当前存档不变。
- [ ] 正常恢复后确认自动“恢复前快照”和同级 `.gameshelf-restore-old-*` 都存在且可读取。
- [ ] 在暂存复制、当前目录重命名和最终替换阶段分别模拟失败，确认当前目录可恢复且没有静默数据丢失。
- [ ] 游戏运行并锁定存档文件时尝试快照/恢复，确认错误可执行且不产生半恢复目录。

## 扩展 Manual：系列与安全视图

- [ ] 调整系列作品顺序，重启后确认顺序与整体完成进度持久化。
- [ ] 分别在搜索、主页、最近游玩、完成时间线、统计、分类、合集、设置计数和批量整理中放置隐藏条目，确认一键安全视图无标题、封面、计数或活动泄漏。
- [ ] 在详情、搜索和敏感合集打开时切换安全视图，确认旧页面、搜索文字和提示立即清除。
- [ ] 验证 GalGame、普通本地游戏和 R18 条目共用同一工作流；R18 只作为分级显示。

## 扩展 Manual：回归与记录

- [ ] 执行 Windows 验证批次 A 的扫描/导入主流程。
- [ ] 运行 `npm run typecheck` 与 `npm test` 并保存输出。
- [ ] 保存每次失败的预览路径、错误信息和 `data\logs\gameshelf.log`；日志对外分享前仍需检查路径隐私。
